# app/

The React Native app half of the **Pear Mobile Starter Kit**.

**Read the root [`../README.md`](../README.md) — not this file — to build and run.**
It covers the mandatory worker step (`cd worker && npm install`, then
`npm run bundle:worker` from here) that the default React Native instructions omit;
skip it and the app boots without its Bare worker bundle.
